function twoSum(arr: number[], target: number) {
	const result = [null, null] as [number | null, number | null];
	for (let i = 0; i < arr.length; i++) {
		const x = arr[i];
		for (let j = i + 1; j < arr.length - i; j++) {
			const y = arr[j];
		}
	}
}

const arr = [1, 2, 3];
const target = 4;
